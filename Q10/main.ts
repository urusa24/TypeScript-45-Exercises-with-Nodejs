//
//Adding Comments: Choose two of the programs you’ve written, and add at least one comment to each.
//If you don’t have anything specific to write because your programs are too simple at this point,
//just add your name and the current date at the top of each program file.
//Then write one sentence describing what the program does.

let whitespaceName = "\n\t Urusa Aazim \t\n";   //Initialize a variable with string value include white spaces using escape sequences

console.log(whitespaceName);                    //Print variable value include white spaces

let withoutwhitespaceName = whitespaceName.trim();//Declare another variable in which we assign a value of old variable that contain without spaces value by using Trim() function  

console.log(withoutwhitespaceName);             //Print new variable value without white spaces